# karadelik public-release allowlist
#
# Consumed by scripts/publish_public.sh. This is a DEFAULT-DENY allowlist:
# anything not listed here is private and never reaches the public repo.
#
# Format: one repo-relative path per line. A trailing "/" copies the
# directory recursively (junk -- __pycache__, *.pyc, *.abs.f, editor swap
# files -- is skipped by the publish script). Blank lines and lines
# starting with "#" are ignored.
#
# NOT listed on purpose:
#   docs/design/index.html  -- publish_public.sh regenerates it in
#                              --public mode directly into the public
#                              worktree (leaf internals stripped).
#   src/rtl/** (except below) -- all leaf + private connectivity RTL.
#   src/tb/, src/fw/, src/llk/, src/toolchain/ -- source, private.
#   *.f filelists, prompts.md, sim_build/, svg_cache/ -- private/generated.

# ── Collateral / documentation ───────────────────────────────────────
README.md
CLAUDE.md
.gitignore
docs/design/.gitignore
scripts/

# ── Public RTL: only the chip wrapper + the shared ABI packages ──────
src/rtl/ktc_chip_top.sv
src/rtl/pkg/ktc_params.sv
src/rtl/pkg/ktc_opcodes.sv
src/rtl/pkg/ktc_types.sv
src/rtl/pkg/ktc_interfaces.sv
